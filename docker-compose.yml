services:
  mcp-server:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      MCP_TRANSPORT: sse
      MCP_PORT: "8000"
    volumes:
      - mcp_cache:/tmp/mcp_cache
    restart: unless-stopped

volumes:
  mcp_cache:
